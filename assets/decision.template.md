<!--
文件名用 YYYY-MM-DD-短横线主题.md（如 2026-08-05-铁律4拆分.md）。
落点：用户项目根目录 docs/decisions/（不是本 skill 的 assets/）。
首次记录时若 docs/decisions/ 不存在，由 AI 直接创建，并同时建 README.md 写明「只追加、旧篇定稿后不改」。
只追加、不修改：新决定开新文件，旧篇定稿后不改。
「被否决的方案」和「不变量（含失效条件）」不许省：它们防止后来的 AI 反复推翻旧决定。
格式范例见本仓库 docs/decisions/。
-->

# （主题）（YYYY-MM-DD）

## 影响

- 之前：（用户视角一两句，尽量带真实数字）
- 之后：（同上）

## 根因

（为什么非改不可。放得下测量数据就放——数字比形容词可信。）

## 裁决

（一句加粗的祈使句：做什么。若与「不变量」第 1 项逐字相同，说明有一个槽位没填对——裁决写动作，不变量写属性。）

## 被否决的方案

（方案名：为什么不选，带上代价——价格、耗时、风险，用数字说。）

## 不变量

1. （什么必须永远为真。）
2. （失效条件：若 X 超过 Y，本裁决失效，届时按数字重估，而不是凭感觉。）
