# install.sh 路径的护栏标注为「不保证生效」（2026-08-01）

## 影响

- 之前：README 没有明确区分「规矩本安装」与「完整安装」。
- 之后：README 明确区分两种安装，并给出 `/hooks` 一条命令的自我确诊方法。

## 根因

护栏是否随 `~/.claude/skills/` 目录安装生效，从未做过真机验证（作者无 Claude Code 环境）。文档不对未实测的事做承诺——宁可保守，不可让用户以为有护栏实际没有。

本文档合并自原 AGENTS.md 第三节第 2 条与第九节第 2 行（同一决定两处记录），迁移时删除第九节重复杂目。

## 裁决

**install.sh 路径的护栏标注为「不保证生效」。**

## 被否决的方案

- 原始记录未载。

## 不变量

1. 未真机验证的机制不做“已生效”承诺。
2. 失效条件：原始记录未载。

